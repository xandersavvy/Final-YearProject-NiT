export const BASE_URL: string = `http://localhost:3010`;
export const JSON_HEADERS: object = {
  headers: { "Content-Type": "application/json" },
};

export const BASE_URL_USER: string = `${BASE_URL}/user`;
export const BASE_URL_DEALER: string = `${BASE_URL}/dealer`;
export const BASE_URL_PRODUCT: string = `${BASE_URL}/product`;
